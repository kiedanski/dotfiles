[Unit]
Description=Sync calendars and contacts with tilde every 15 minutes

[Timer]
OnBootSec=2min
OnUnitActiveSec=15min
# Tablet sleeps a lot — run a catch-up sync on resume rather than skipping.
Persistent=true
AccuracySec=1min

[Install]
WantedBy=timers.target
